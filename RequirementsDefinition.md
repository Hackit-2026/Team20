# アプリ総合要件定義書 (Requirements Definition - 最新統合確定版)

本ドキュメントは、アプリ「ヤニモグラ」の全機能仕様・計算公式・画面遷移・デバッグ機能要件をまとめた仕様書です。
※本ドキュメントでは「AI」「人工知能」という表現を完全に排除しています。

---

## 1. アプリ概要 & コアコンセプト
- **アプリ名**: **ヤニモグラ (YANI-GOTCHI)**
- **コンセプト**: 「タバコを促進してキャラを育てる反面教師育成 ＆ 禁煙・減煙支援アプリ」

---

## 2. 📱 画面構成 ＆ ナビゲーション分離仕様【最新要件】
メイン画面をシンプルかつ使いやすく保つため、機能ごとにそれぞれ**独立した別画面**へ分離・遷移する。

- **メイン画面 (`HomeScreen.kt`)**:
  - 本日の日付 (M/d)
  - 本日の申告状況 ＆ 目標残り日数
  - 本数入力 (＋/− ボタン) ＆ 保存ボタン
  - 上部ナビゲーションボタン（「📊 履歴」「🎯 減煙設定」「🛠️ デバッグ」）
- **履歴画面 (`HistoryScreen.kt`)**: 独立画面。過去の全申告ログ一覧（日付・吸った/吸わなかった・本数）を表示。
- **減煙設定画面 (`GoalSettingScreen.kt`)**: 独立画面。重み付き平均分析 ＆ きつさの値設定。
- **デバッグ画面 (`DebugScreen.kt`)**: 独立画面。指定日付登録・30日デモデータ挿入・通知時間設定・全リセット。

---

## 3. 喫煙量分析 ＆ 段階的減煙システム【要件拡張】

### 3-1. 先日から1週間前までのタバコの吸った量の重みづけ (Weighted Average)
- **計算ロジック**:
  - 直近7日間（昨日〜7日前）の吸った本数に対し、新しい日ほど大きな重み（昨日=1.0, 2日前=0.85, 3日前=0.7, 4日前=0.55, 5日前=0.4, 6日前=0.25, 7日前=0.1）を乗算して「重み付き平均本数」を算出する。

### 3-2. 目標を少しずつ減らす機能 (段階的減煙アルゴリズム ＆ 0.2～20.0 自由選択)
- **計算公式**:
  $$\text{次の目標} = \text{floor}(\text{今週の1日あたり平均吸った本数} - \text{ユーザーが決めたきつさの値})$$
- **選択範囲仕様**:
  - **ユーザーが決めたきつさの値（難易度）**: **`0.2` 〜 `20.0`** の範囲で 0.1 刻みで自由に調整・選択可能。
- **制御規則**:
  - 切り捨て (`floor`) 処理を実施。
  - **特別例外条件**: 今回の目標が `0` であれば、無条件に `0` で出力する（一度 0 本の完全禁煙に到達した目標は 0 を維持する）。

---

## 4. デバッグ ＆ 運用機能仕様

### 4-1. 過去30日間のデモデータ一括投入 (デバッグ用)
- 過去30日間の申告ログを一括生成して履歴に反映。

### 4-2. 何月何日を選択してタバコのデータを入れる機能 (デバッグ用)
- 任意の年月日（`YYYY-MM-DD` 形式）および本数を指定して過去ログを追加・更新可能。

### 4-3. 通知時刻のカスタマイズ設定
- リマインド通知時刻（時・分）を任意に変更・スケジュール登録可能。

### 4-4. すべてのデータをリセットし始めからスタートする機能
- ローカルストレージデータをクリアし、完全初期状態へ復帰する。

---

## 5. 他アプリ起動時の警告オーバーレイペナルティ機能
- 本日「吸った」と申告済みの場合に他アプリ（Chrome 等）を開いた瞬間、重ねて警告画面（⚠ ペナルティ中）をオーバーレイ表示。