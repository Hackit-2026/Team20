# アプリ総合要件定義書 (Requirements Definition - 最新統合確定版)

本ドキュメントは、アプリ「ヤニモグラ」の全機能仕様・計算公式・デバッグ機能要件をまとめた仕様書です。
※本ドキュメントでは「AI」「人工知能」という表現を完全に排除しています。

---

## 1. アプリ概要 & コアコンセプト
- **アプリ名**: **ヤニモグラ (YANI-GOTCHI)**
- **コンセプト**: 「タバコを促進してキャラを育てる反面教師育成 ＆ 禁煙・減煙支援アプリ」

---

## 2. 喫煙量分析 ＆ 段階的減煙システム【新要件】

### 2-1. 先日から1週間前までのタバコの吸った量の重みづけ (Weighted Average)
- **計算ロジック**:
  - 直近7日間（昨日〜7日前）の吸った本数に対し、新しい日ほど大きな重み（昨日=1.0, 2日前=0.85, 3日前=0.7, 4日前=0.55, 5日前=0.4, 6日前=0.25, 7日前=0.1）を乗算して「重み付き平均本数」を算出する。
  - 最新の喫煙ペースの変化を感度高く数値化して表示する。

### 2-2. 目標を少しずつ減らす機能 (段階的減煙アルゴリズム)
- **計算公式**:
  $$\text{次の目標} = \text{floor}(\text{今週の1日あたり平均吸った本数} - \text{ユーザーが決めたきつさの値})$$
- **制御規則**:
  - 切り捨て (`floor`) 処理を実施。
  - **特別例外条件**: 今回の目標が `0` であれば、無条件に `0` で出力する（一度 0 本の完全禁煙に到達した目標は 0 を維持する）。

---

## 3. デバッグ ＆ 運用機能仕様【新要件】

### 3-1. 過去30日間のデモデータ一括投入 (デバッグ用)
- デバッグパネル内に「過去30日分のデモデータを投入」ボタンを設置。
- 過去30日間の申告ログ（吸った/吸わなかったおよび本数）を一括生成して履歴に反映。

### 3-2. 何月何日を選択してタバコのデータを入れる機能 (デバッグ用)
- 任意の年月日（`YYYY-MM-DD` 形式）および「吸った本数」を入力・指定して過去ログを追加・更新可能。

### 3-3. 通知時刻のカスタマイズ設定
- デバッグ・詳細設定パネルにて、未申告リマインド通知を発火させる時刻（時・分）を任意に変更・スケジュール登録可能。

### 3-4. すべてのデータをリセットし始めからスタートする機能
- 「すべてのデータをリセットして最初からスタート」ボタンを配置。
- `app_prefs` 内のローカルストレージデータをクリアし、完全初期状態へ復帰する。

---

## 4. 他アプリ起動時の警告オーバーレイペナルティ機能
- **機能概要**: `SYSTEM_ALERT_WINDOW` および `PACKAGE_USAGE_STATS` 権限を使用し、本日「吸った」と申告済みの場合に他アプリ（Chrome 等）を開いた瞬間、重ねて警告画面（⚠ ペナルティ中）をオーバーレイ表示。