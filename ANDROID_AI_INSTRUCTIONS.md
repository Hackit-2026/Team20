# 📱 Android開発AI専用 指示書 (Android App Developer Guide)

このドキュメントは、AndroidApp (Kotlin / Jetpack Compose) の開発を担当するAIエージェントおよび開発者向けの**開発指示書**です。以下の要件に従ってコードを生成・リファクタリングしてください。
※アプリ内およびUI上では「AI」「人工知能」という表記は一切使用せず、「コミュニティ仲間」「専属メンバー」と表記してください。

---

## 🎨 担当 B (画面 & キャラクター表現) 編集限定領域ガイド

担当 B は、UIデザイン、ビジュアル演出、カラーパレット、アニメーション、画面配置に特化して以下のファイル群を自由に編集・拡張できます。コアロジック（`Repo.kt`, `MainViewModel.kt`, `StageLogic.kt`）とは `UiState` およびイベントコールバック経由で疎結合になっているため、UIの見た目を安全にブラッシュアップ可能です。

### 📁 担当 B 専属編集ファイル一覧 (`AndroidApp/app/src/main/java/com/example/myapplication/ui/`)
1. **`HomeScreen.kt`**: メイン画面レイアウト（鏡モチーフフレーム、残り日数強調カード、ステッパー、カラーパレット `ScreenBg`, `Plate`, `Accent`, `Ink`）
2. **`CharacterAssets.kt`**: 21段階 (S0 〜 S20) キャラクター描画・鏡ベゼル・アンビエントグロー演出 (`CharacterView`)
3. **`OnboardingScreen.kt`**: チャレンジ期間選択・目標本数設定（初期値10本）のビジュアルUI
4. **`ResultScreen.kt`**: 最終結果発表・サマリーカード画面UI
5. **`CalendarScreen.kt`**: 閲覧履歴・週(7日)/月(30日)可視化棒グラフ描画 (`SmokingHistoryChart`)
6. **`CommunityFeedScreen.kt`**: コミュニティタイムラインカード ＆ 4人メッセージ吹き出しUI
7. **`StartScreen.kt`**: 初回起動「＋」ボタン付きスタート画面UI

---

## 🎯 アプリ概要 & 通信仕様
- **アプリ名**: **YANI-GOTCHI / ヤニモグラ**
- **アーキテクチャ**: Android Jetpack Compose + MVVM (ViewModel, Repository)
- **バックエンド通信**: Python (FastAPI + LM Studio `google/gemma-4-12b-qat`) サーバー API
  - **優先サーバーIP**: `http://192.168.25.42:8000/api/` (エミュレータ用 fallback: `http://10.0.2.2:8000/api/`)

---

## 📋 画面別・ロジック仕様要件

### 1. 初期化・リセット仕様 (`data/Repo.kt`)
- **リセット時・初期設定時の目標本数初期値**: **`10本`** (dailyGoal = 10)

### 2. メイン画面の表示仕様 (`ui/HomeScreen.kt`, `ui/MainViewModel.kt`)
- **「本日の本数」初期表示値**: オンボーディングまたは設定で保持されている**「1日の目標本数 (10本など)」をそのまま初期値**としてセットする。
- **減算（下げる方向）の心理的調整UX**:
  - 本日の本数を「目標本数」から `+` / `-` ボタンで**下げる方向へ数値を調整**させることで、我慢・減煙できている達成感を高めるUI設計とする。
- **ボタン配置**:
  - **左上**: 「履歴ボタン（`CalendarScreen` へ）」
  - **中央横**: 「コミュニティボタン（`CommunityFeedScreen` へ）」
  - **右上**: 「デバッグボタン（`DebugScreen` へ）」
- **確定保存ボタン**: **1日に1回のみクリック可能**（`isConfirmedToday` フラグ制御）。
- **中央キャラクター画像**: 鏡モチーフベゼル + アンビエントグロー演出で大きく配置。

### 3. デバッグ画面 (`ui/DebugScreen.kt`)
- メイン画面の**「右上ボタン」**から遷移。
- **`ResultScreen.kt` への遷移ボタンはデバッグ画面の中に配置**。
- 🚨 1日放置ペナルティのテスト実行ボタン (`+10pt`)。
- サーバーURL自由変更入力欄。
- スマートウォッチ模擬テストボタン。

### 4. コミュニティタイムライン画面 (`ui/CommunityFeedScreen.kt`)
- Pythonサーバー (`http://192.168.25.42:8000/api/feed`) からタイムライン投稿を取得。
- 投稿時、待ち時間ゼロ（0.05秒）で即時ローカル表示 ＆ バックグラウンド非同期でLM Studio 4人のコメントを生成・SQLite保存。
- 右上の「🔄 リロード」ボタンを押すことで、生成された4人の専属メンバー（熱血仲間・修造🔥、ツンデレ友達・アスカ😳、Dr.ヘルス👨‍⚕️、ヤニモグラ👹）からのリアルタイム応答コメントを一括取得・表示。
- **画面上やセリフに「AI」という文字は一切含めないこと**。
